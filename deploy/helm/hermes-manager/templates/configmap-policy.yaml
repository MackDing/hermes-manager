apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hermesmanager.fullname" . }}-policy
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hermesmanager.labels" . | nindent 4 }}
data:
  policy.yaml: |
    {{- .Values.policy | nindent 4 }}
