apiVersion: v1
kind: Service
metadata:
  name: {{ include "hermesmanager.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hermesmanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hermesmanager.selectorLabels" . | nindent 4 }}
